import React, { Component} from "react";
import "./App.css";
import {hot} from "react-hot-loader"

function App(){
    return(
      <button>
        카카카오 공유
      </button>
    );
  
}

export default hot(module)(App);